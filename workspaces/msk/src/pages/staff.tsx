import { BaseLayout } from '@layouts';
// import { useEffect } from 'react';

export const StaffPage = (props: { socket: any }) => {
	// useEffect(() => {
	// 	props.socket.on('receive_request', (data: any) => {
	// 		console.log(data);
	//     setMessageList((list) => [...list, data]);
	// });
	// }, [props.socket]);

	return (
		<BaseLayout>
			<div>hmm</div>
		</BaseLayout>
	);
};
